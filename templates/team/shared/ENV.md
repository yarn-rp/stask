# Environment Variables

_Fill in during project setup. Document all required environment variables._

## Required Variables

| Variable | Required | Description |
|----------|----------|-------------|
| | | |

## Rules

- Never commit `.env.local` to git
- Never use secret keys in client components
- Variables prefixed `NEXT_PUBLIC_` are exposed to the browser — treat them as public
- For production, set all vars in your hosting dashboard
